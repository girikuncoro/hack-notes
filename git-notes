# Add remote url
git remote add origin https://github.com/user/repo.git

# Reset pushed commit one level before
git reset HEAD^

# Undo pushed merge commit
git revert -m 1 <merge-commit>

# Undo git add
git reset filename.txt

# Stash specific file/folder
git stash push -m <label> <file/folder>

# Delete local branch
git branch -d local-branch

# Rename current branch
git branch -m <newname>
